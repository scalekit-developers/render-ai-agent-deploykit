OPENAI_API_KEY=your-openai-api-key

# LiteLLM proxy (optional — if set, takes precedence over OPENAI_API_KEY)
LITELLM_API_KEY=
LITELLM_BASE_URL=https://llm.scalekit.cloud
LITELLM_MODEL=claude-sonnet-4-6

RENDER_API_KEY=your-render-api-key
RENDER_WORKFLOW_SLUG=docfix

# Linear webhook (from Linear workspace settings → Webhooks)
LINEAR_WEBHOOK_SECRET=your-linear-webhook-secret
# Scalekit identifier for the bot/service Linear account
# Pre-connect a Linear account in Scalekit under this identifier (e.g. "bot")
# Used for posting comments without requiring the user's OAuth
SCALEKIT_BOT_IDENTIFIER=bot

# Scalekit Agent Auth
SCALEKIT_ENVIRONMENT_URL=https://your-env.scalekit.com
SCALEKIT_CLIENT_ID=your-scalekit-client-id
SCALEKIT_CLIENT_SECRET=your-scalekit-client-secret

# Webhook + auth service
AUTH_BASE_URL=https://your-webhook-service.onrender.com
WEBHOOK_PORT=3001
